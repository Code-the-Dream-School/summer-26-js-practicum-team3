# Port the frontend dev server runs on
VITE_PORT=8081

# Address of the backend; Vite forwards /api requests here in dev
VITE_TARGET="http://localhost:8080"

# Leave empty (same address as the frontend)
VITE_API_ORIGIN=""
