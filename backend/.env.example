DATABASE_URL=postgresql://user:password@localhost:5432/mydb
PORT=8080
JWT_SECRET=your-long-random-secret-here

# Optional — local DB testing (Scenario D). DATABASE_URL above stays untouched;
# uncomment both lines below to point the app at your local database instead.
# LOCAL_DATABASE_URL=postgresql://user@localhost:5432/my_local_db
# DB_TARGET=local
